"use client";
import { useState } from "react";
import { apiClient } from "../lib/api-client";
import { Button,Logo,useHydrated } from "./ui";
export function AdminLoginV1(){const hydrated=useHydrated();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[transaction,setTransaction]=useState("");const[code,setCode]=useState("");const[error,setError]=useState("");const[working,setWorking]=useState(false);const request=async()=>{if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||password.length<8){setError("Enter the approved Master Admin email and a valid password.");return;}try{setWorking(true);setError("");const r=await apiClient<{transactionId:string}>("/api/auth/request-otp",{method:"POST",body:JSON.stringify({flow:"SIGN_IN",role:"SUPER_ADMIN",email:email.trim(),password})});setTransaction(r.transactionId);}catch(e){setError(e instanceof Error?e.message:"Secure verification could not be started.");}finally{setWorking(false)}};const verify=async()=>{try{setWorking(true);setError("");const r=await apiClient<{authenticated:boolean;redirectTo:string}>("/api/auth/verify-otp",{method:"POST",body:JSON.stringify({transactionId:transaction,channel:"EMAIL",code})});if(r.authenticated)window.location.assign(r.redirectTo);}catch(e){setError(e instanceof Error?e.message:"Verification failed.");}finally{setWorking(false)}};return <main className="auth-page-admin-v1"><section className="admin-login-card" aria-busy={!hydrated}><Logo/><span className="eyebrow">Super admin only</span><h1>Master Access</h1><p>Protected platform operations require password and email OTP verification. Administrative activity is audited.</p>{!hydrated?<p role="status">Preparing secure form…</p>:transaction?<form onSubmit={e=>{e.preventDefault();void verify()}}><label className="auth-field"><span>Email OTP</span><input aria-label="Email OTP" inputMode="numeric" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,"").slice(0,6))}/></label>{error&&<p className="workflow-error" role="alert">{error}</p>}<Button type="submit" disabled={code.length!==6||working}>{working?"Verifying…":"Verify and open Master Access"}</Button></form>:<form onSubmit={e=>{e.preventDefault();void request()}}><label className="auth-field"><span>Master email</span><input aria-label="Master email" type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="auth-field"><span>Password</span><input aria-label="Password" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="workflow-error" role="alert">{error}</p>}<Button type="submit" disabled={working}>{working?"Preparing secure verification…":"Continue to OTP"}</Button></form>}</section></main>}
