# The real runtime/migration.env contains only the controlled migration role URL.
# It is generated from SSM with mode 0600 and is never exposed to the backend.
DATABASE_URL=postgres://sapienworx_migrator:REDACTED@rds-endpoint:5432/sapienworx?sslmode=require
