# Copy to .env for local overrides. Do not use these development defaults outside your machine.
POSTGRES_PASSWORD=replace-with-a-local-password
RABBITMQ_PASSWORD=replace-with-a-local-password
RABBITMQ_USERNAME=sapienworx
# QA-only overrides used by the isolated Compose environment. Never use these
# values in a shared or production deployment.
QA_TEST_ACCOUNT_PASSWORD=SapienworxQA!2026
QA_STATIC_OTP=999999
# Base64 of at least 32 cryptographically random bytes.
JWT_BASE64_SECRET=replace-with-a-32-byte-base64-secret
