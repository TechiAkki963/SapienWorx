"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { apiRequest } from "@/lib/api";
import { Button } from "@/components/ui/button";
export function CreateJobForm(){const router=useRouter();const [open,setOpen]=useState(false);const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const d=new FormData(e.currentTarget);try{await apiRequest("/api/v1/recruiter/jobs",{method:"POST",body:JSON.stringify({title:d.get("title"),department:d.get("department"),description:d.get("description"),employment_type:d.get("employment_type"),work_mode:d.get("work_mode"),city:d.get("city"),state:d.get("state"),country_code:"IN",min_experience_months:Number(d.get("min_experience_months")),openings:Number(d.get("openings")),application_deadline:d.get("application_deadline")||null,publish:d.get("publish")==="on"})});setOpen(false);router.refresh();}catch(c){setError(c instanceof Error?c.message:"Could not create job.");}finally{setBusy(false)}}if(!open)return <Button onClick={()=>setOpen(true)}>Post a job</Button>;return <form onSubmit={submit} className="rounded-2xl border border-indigo/20 bg-indigo-soft/25 p-4"><div className="flex items-center justify-between gap-3"><h2 className="font-bold">Create job</h2><button type="button" onClick={()=>setOpen(false)} className="text-sm font-semibold text-ink-muted hover:text-ink">Close</button></div>{error&&<p role="alert" className="mt-3 rounded-lg bg-red-50 p-2 text-sm text-red-700">{error}</p>}<div className="mt-4 grid gap-3 md:grid-cols-2"><input name="title" required placeholder="Job title" className="rounded-xl border border-line bg-white px-3 py-2.5"/><input name="department" placeholder="Department" className="rounded-xl border border-line bg-white px-3 py-2.5"/><select name="employment_type" className="rounded-xl border border-line bg-white px-3 py-2.5"><option value="full_time">Full time</option><option value="contract">Contract</option><option value="internship">Internship</option><option value="part_time">Part time</option><option value="temporary">Temporary</option></select><select name="work_mode" className="rounded-xl border border-line bg-white px-3 py-2.5"><option value="onsite">Onsite</option><option value="hybrid">Hybrid</option><option value="remote">Remote</option></select><input name="city" placeholder="City" className="rounded-xl border border-line bg-white px-3 py-2.5"/><input name="state" placeholder="State" className="rounded-xl border border-line bg-white px-3 py-2.5"/><input name="min_experience_months" type="number" min="0" defaultValue="0" placeholder="Min experience months" className="rounded-xl border border-line bg-white px-3 py-2.5"/><input name="openings" type="number" min="1" defaultValue="1" className="rounded-xl border border-line bg-white px-3 py-2.5"/><input name="application_deadline" type="date" className="rounded-xl border border-line bg-white px-3 py-2.5 md:col-span-2"/><textarea name="description" required rows={5} placeholder="Job description" className="rounded-xl border border-line bg-white px-3 py-2.5 md:col-span-2"/></div><label className="mt-3 flex items-center gap-2 text-sm font-semibold"><input type="checkbox" name="publish"/>Publish immediately</label><Button className="mt-4" type="submit" disabled={busy}>{busy?"Saving…":"Create job"}</Button></form>}
