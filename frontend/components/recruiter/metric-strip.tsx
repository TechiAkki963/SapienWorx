export function MetricStrip({items}:{items:{label:string;value:string|number;hint?:string}[]}){return <div className="grid gap-px overflow-hidden rounded-2xl border border-line/70 bg-line/70 sm:grid-cols-2 xl:grid-cols-4">{items.map(item=><div key={item.label} className="bg-white px-4 py-3"><p className="text-xs font-bold uppercase tracking-[0.09em] text-ink-muted">{item.label}</p><div className="mt-1 flex items-end justify-between gap-2"><p className="text-2xl font-bold tracking-[-0.04em] text-ink">{item.value}</p>{item.hint&&<span className="text-xs text-ink-muted">{item.hint}</span>}</div></div>)}</div>}
