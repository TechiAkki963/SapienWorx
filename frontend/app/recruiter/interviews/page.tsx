import { RecruiterShell } from "@/components/recruiter/recruiter-shell";
import { ScheduleInterviewForm } from "@/components/recruiter/schedule-interview-form";
import { requireRole } from "@/lib/auth-server";
import { Interview, label, PipelineRow } from "@/lib/recruiter";
import { recruiterAPI } from "@/lib/recruiter-server";
export const dynamic="force-dynamic";
export default async function InterviewsPage(){await requireRole("recruiter");const [{items},{items:applications}]=await Promise.all([recruiterAPI<{items:Interview[]}>("/api/v1/recruiter/interviews"),recruiterAPI<{items:PipelineRow[]}>("/api/v1/recruiter/pipeline?limit=100")]);return <RecruiterShell><div className="grid gap-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.12em] text-indigo">Interview operations</p><h1 className="mt-1 text-2xl font-bold tracking-[-0.035em] sm:text-3xl">Interviews</h1><p className="mt-1 text-sm text-ink-muted">Schedule with an externally created meeting URL; SapienWorx stores and opens the link only.</p></div><ScheduleInterviewForm applications={applications}/></div>{items.length?<div className="overflow-x-auto rounded-2xl border border-line/70 bg-white"><table className="min-w-[900px] w-full text-left text-sm"><thead className="bg-[#f8f9fc] text-[11px] uppercase tracking-[0.08em] text-ink-muted"><tr><th className="px-3 py-2.5">Candidate</th><th className="px-3 py-2.5">Job</th><th className="px-3 py-2.5">Schedule</th><th className="px-3 py-2.5">Duration</th><th className="px-3 py-2.5">Status</th><th className="px-3 py-2.5">Meeting</th></tr></thead><tbody>{items.map(i=><tr key={i.id} className="border-t border-line/55 hover:bg-indigo-soft/20"><td className="px-3 py-3 font-bold">{i.candidate_name}</td><td className="px-3 py-3">{i.job_title}</td><td className="px-3 py-3 text-ink-muted">{new Intl.DateTimeFormat("en-IN",{dateStyle:"medium",timeStyle:"short"}).format(new Date(i.scheduled_at))}</td><td className="px-3 py-3 text-ink-muted">{i.duration_minutes} min</td><td className="px-3 py-3"><span className="rounded-full bg-indigo-soft px-2 py-1 text-xs font-bold">{label(i.status)}</span></td><td className="px-3 py-3"><a href={i.meeting_url} target="_blank" rel="noopener noreferrer" className="font-bold text-indigo hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo">Join ↗</a></td></tr>)}</tbody></table></div>:<div className="rounded-2xl border border-dashed border-line bg-white p-10 text-center"><h2 className="font-bold">No interviews scheduled</h2><p className="mt-2 text-sm text-ink-muted">Select an application and attach your external meeting URL.</p></div>}</div></RecruiterShell>}
